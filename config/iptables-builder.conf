[main]
data = /etc/sysconfig/iptables

[sources]
basepath = /etc/iptables-builder
availablepath = $basepath/available.d
enabledpath = $basepath/enabled.d

[chains]
input = INPUT
output = OUTPUT
forward = FORWARD
